import { Login } from 'react-admin'
import React from 'react'

const loginStyles = {
  backgroundSize: '100%',
  backgroundRepeat: 'round'
}

const LoginPage = () => <Login style={loginStyles} />
export default LoginPage
